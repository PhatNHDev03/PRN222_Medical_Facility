@page
@model MedicaiFacility.RazorPage.Pages.MedicalExperts.DeleteModel

@{
    ViewData["Title"] = "Delete";
    Layout = null;
}

<h4>Are you sure you want to delete this Expert?</h4>

<form method="post" asp-page="Delete" asp-route-id="@Model.MedicalExpert.ExpertId">
    <input type="hidden" asp-for="MedicalExpert.ExpertId" />
    <button type="submit" class="btn btn-danger">Delete</button>
    <a asp-page="Index" class="btn btn-secondary">Cancel</a>
</form>

@if (TempData["ErrorMessage"] != null)
{
    <div class="alert alert-danger text-center mt-3">@TempData["ErrorMessage"]</div>
}
@if (TempData["SuccessMessage"] != null)
{
    <div class="alert alert-success text-center mt-3">@TempData["SuccessMessage"]</div>
}